---
date: <YYYY-MM-DD>
workshop: <source workshop slug>
practices: [<practice slugs this lesson is about; may be empty if theme-only>]
themes: [<free-form tags, e.g. power-dynamics, remote, large-group, timing>]
---

<One-line lesson — the durable takeaway, phrased so future-you can act on it.>

<Optional: a sentence or two of detail / what to do differently next time.>

<!--
One file per lesson: lessons-learned/<date>-<short-kebab>.md (the feedback agent names it).
- `practices` ties the lesson to structures by slug so the future lessons-loop can surface
  "how `triz` lands with execs". Any slug listed MUST resolve to a real practices/<slug>.md.
- `themes` are free-form; they let the loop surface "lessons about spicy power-dynamics rooms".
- No clear practice? Leave `practices: []` and rely on `themes`. Never invent a slug.
-->
