---
slug: <same slug as the design>
prep_mode: <in-person | digital>
prepared: <YYYY-MM-DD>
coverage: <N>/<N>            # structures with setup steps / total structures in the design
---

# Setup — <workshop name>

<!-- The executor agent fills this from design.md + the practice files. Tick as you prep. -->

<!-- ============ IN-PERSON PACK ============ (use this block when prep_mode: in-person) -->

## Materials checklist
- [ ] <item aggregated across structures — stickies, markers, paper, timer, printed cards…>
- [ ] <…>

## Per-structure setup
### <block> — <Structure Name> (`<slug>`)
- <concrete in-person setup from the practice's Steps / Group config / Medium fit (In-person)>

<!-- ============ DIGITAL PACK (Miro + video) ============ (use this block when prep_mode: digital) -->
<!-- NOTE at top of a digital setup.md: "This is a recipe to build yourself — not a live Miro board." -->

## Pre-build checklist
- [ ] <Miro frame / sticky area / breakout config / shared doc to create>
- [ ] <…>

## Per-structure setup
### <block> — <Structure Name> (`<slug>`)
- **Miro:** <frame recipe — frames, sticky zones — from Medium fit (Miro)>
- **Video:** <breakout/timer notes from Medium fit (Video)>

<!--
Cite structures by slug (matches the design). Any flagged default the executor couldn't ground
is marked "⚠ check". Prep happens before the session — this file does not change the brief status.
-->
