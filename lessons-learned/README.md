# Lessons learned (private)

Your aggregated, reusable lessons from running real workshops live here. **This folder's contents are
gitignored** — only this README is committed, so a fresh clone has the folder but none of your private
notes.

Future `feedback` + `lessons-loop` features will write here, and the `designer` will read from here so
your designs improve over time. For now it's a place to drop your own notes.
